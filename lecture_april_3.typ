We make RSA semantically secure by introducing randomness into the cyrptosystem, adding a random oracle $G: ZZ^k_2 -> ZZ_2^m$ into the public key. Let $PP = ZZ_2^m, e = ZZ_2^k crossmark ZZ_2^m$, and define $ e_k(x) = r^b mod n, G(r) xor x) $ $ (y_1, y_2) in ZZ_2^k crossmark ZZ_2^m $ for random $r in ZZ_2^k$ and $d_k((y_1, y_2)) = G(y_1^a mod n) xor y_2$. This work, since $d_k(y_1, y_2)$ equals $ G((r^b mod n)^a mod n) xor G(r) xor x $ $ G(r) xor G(r) xor x $ $ = x $ since $r^(a b) = r$. An informal argument why this semantically secure (i.e, the distinguishing problem can't be solved with probability more than $1/2$) is that in order to determine any information aboue $x$ we must determine the mask $G(r)$. Any partial information about $r$ is useless because $G$ is a random oracle the only way to compute $G(r)$ is to determine $r$. Under the assumption that RSA is secure this argumented cryptosystem is semantically secure. The main drawback is data expansion: $m$ bit of plaintext expand to $m+K$ bits of ciphertext. 

*The Discreate log problem*: say $G$ is a group, $alpha in G$ of order n, and define $<alpha> = {alpha^i: 0<=i<=n-1}$ to be the cyclic group generated by $alpha$. For instance $G = ZZ_p^*$ where $p$ is prime, and $alpha$ is a primitive element of $ZZ^*_p$, i.e $<alpha> = ZZ_p^*$. The discrete log problem is given $beta in <a>$ to determine the value of $i$ for which $beta = alpha^i$, i.e., compute $i = log_alpha(beta)$ the dicrete log of $beta$ base $alpha$. Ex. take $p = 2579$ and $alpha = 2$, a primitive element in $ZZ_p^*$. What is $log_2(949) "in" ZZ_p^* ?$. In constrast to logs over the reals, computing logs in $ZZ_p^*$ seems difficult in general. The naive stratergy would be to compute $2^2, 2^3, 2^4, ..., 2^(p-2) (mod p)$ until $949$ is reached. In the worst case, this uses at most $p$ evaluations of $alpha mod p$. Since each multiplication $mod p$ is $O((log p)^2)$ bit ops, this uses $O(p(log p)^2)$ bit ops $O(2^log p(log p)^2))$.
